/**
 * Spanish Pre-A1 content layer — Phase 20 layout.
 *
 * Structure:
 *   seed.ts           → course / units / competencies (structure)
 *   seedSublessons.ts → 9-stage engine + assessment (depth)
 *   content/phases/   → authored overrides per unit (incremental)
 *
 * Future: move phase files here as content grows:
 *   content/spanish/pre-a1/phases/preA1.phaseNN.ts
 */
export { phases } from '../../phases'
